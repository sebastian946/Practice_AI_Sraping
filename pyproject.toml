[project]
name = "practice-ai-scraping"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "openai>=1.109.1",
    "playwright>=1.49.0",
    "pytest-playwright>=0.5.0",
]

[tool.pytest.ini_options]
addopts="--browser chromium --headed"
